# One-Hub 项目开发规范

## 项目概述

One-Hub 是一个基于 React 的现代化 API 管理平台，采用企业级设计系统，提供专业、清洁的用户界面和优秀的用户体验。

## 技术栈规范

### 核心技术栈
- **前端框架**: React 18 + Vite
- **样式系统**: Tailwind CSS 4.x (CSS变量 + OKLCH色彩空间)
- **UI组件库**: shadcn/ui (new-york style)
- **图标库**: lucide-react (主力)，逐步替换 @iconify/react
- **图表库**: recharts (统一使用，逐步替换 apexcharts)
- **状态管理**: Redux Toolkit
- **路由**: React Router v6
- **国际化**: react-i18next
- **构建工具**: Vite + @tailwindcss/vite

### 开发工具
- **代码格式化**: Prettier + ESLint
- **类型检查**: PropTypes (逐步迁移到 TypeScript)
- **包管理**: Yarn
- **CSS工具**: clsx + tailwind-merge

## 代码风格规范

### 文件结构
```
src/
├── components/          # 可复用组件
│   └── ui/             # shadcn/ui 基础组件
├── views/              # 页面组件
├── hooks/              # 自定义 Hooks
├── lib/                # 工具函数
├── assets/             # 静态资源
└── store/              # Redux 状态管理
```

### 命名规范
- **组件文件**: PascalCase (如 `ModelUsagePieChart.jsx`)
- **工具函数**: camelCase (如 `calculateQuota`)
- **常量**: UPPER_SNAKE_CASE (如 `API_BASE_URL`)
- **CSS类名**: 使用 Tailwind 工具类，自定义类用 kebab-case

### 导入顺序
1. React 相关导入
2. 第三方库导入
3. 项目内部导入 (按路径深度排序)
4. 类型导入 (如果使用 TypeScript)

```javascript
// 示例
import React from 'react';
import { PieChart, Pie, Cell } from 'recharts';
import { Card, CardContent, CardHeader } from '@/components/ui/card';
import { useTranslation } from 'react-i18next';
import { calculateQuota } from 'utils/common';
```

## UI/UX 设计规范

### 色彩系统 (OKLCH + CSS变量)

#### 基础色彩令牌
```css
/* 亮色模式 */
--background: oklch(1 0 0);                    /* 纯白背景 */
--foreground: oklch(0.147 0.004 49.25);        /* 深色文字 */
--primary: oklch(0.216 0.006 56.043);          /* 主色调 */
--secondary: oklch(0.97 0.001 106.424);        /* 次要色 */
--muted: oklch(0.97 0.001 106.424);            /* 静音色 */
--accent: oklch(0.97 0.001 106.424);           /* 强调色 */
--destructive: oklch(0.577 0.245 27.325);      /* 危险色 */
--border: oklch(0.923 0.003 48.717);           /* 边框色 */

/* 暗色模式 */
.dark {
  --background: oklch(0.147 0.004 49.25);      /* 深色背景 */
  --foreground: oklch(0.985 0.001 106.423);    /* 亮色文字 */
  --card: oklch(0.216 0.006 56.043);           /* 卡片背景 */
}
```

#### 图表色彩
```css
--chart-1: oklch(0.646 0.222 41.116);  /* 珊瑚色 */
--chart-2: oklch(0.6 0.118 184.704);   /* 青色 */
--chart-3: oklch(0.398 0.07 227.392);  /* 海军蓝 */
--chart-4: oklch(0.828 0.189 84.429);  /* 黄色 */
--chart-5: oklch(0.769 0.188 70.08);   /* 橙色 */
```

### 布局系统

#### 间距规范
- **页面间距**: `space-y-8` (32px)
- **区块间距**: `space-y-6` (24px)
- **组件间距**: `gap-6` (24px)
- **卡片内边距**: `px-6 py-6` (24px)

#### 响应式断点
```css
/* Mobile-first 断点 */
sm: 640px   /* 小屏设备 */
md: 768px   /* 平板 */
lg: 1024px  /* 桌面 */
xl: 1280px  /* 大屏 */
```

#### 网格系统
```jsx
// 标准网格布局
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
  {/* 响应式卡片网格 */}
</div>
```

### 组件设计规范

#### Card 组件
```jsx
<Card className="overflow-hidden">
  <CardHeader>
    <CardTitle className="text-xl font-semibold">
      {/* 标题使用 font-semibold */}
    </CardTitle>
  </CardHeader>
  <CardContent>
    {/* 内容区域 */}
  </CardContent>
</Card>
```

**Card 样式规范**:
- 圆角: `rounded-xl` (12px)
- 边框: `border` (1px)
- 阴影: `shadow-sm`
- 内边距: `px-6 py-6`

#### Button 组件
```jsx
<Button variant="default" size="default">
  {/* 按钮内容 */}
</Button>
```

**Button 变体**:
- `default`: 主要按钮
- `outline`: 次要按钮
- `ghost`: 幽灵按钮
- `destructive`: 危险操作

**Button 尺寸**:
- `sm`: h-8 (32px)
- `default`: h-9 (36px)
- `lg`: h-10 (40px)
- `icon`: 图标按钮

#### Typography 规范
```jsx
// 页面标题
<h1 className="text-4xl font-bold tracking-tight">
  {/* 页面主标题 */}
</h1>

// 卡片标题
<CardTitle className="text-xl font-semibold">
  {/* 卡片标题 */}
</CardTitle>

// 描述文字
<p className="text-base text-muted-foreground">
  {/* 描述性文字 */}
</p>

// 小标签
<span className="text-sm text-muted-foreground">
  {/* 辅助信息 */}
</span>
```

### 图表设计规范

#### 使用 recharts 统一图表库
```jsx
import { PieChart, Pie, Cell, ResponsiveContainer, Legend, Tooltip } from 'recharts';

// 标准图表容器
<ResponsiveContainer width="100%" height={300}>
  <PieChart>
    <Pie
      data={data}
      cx="50%"
      cy="50%"
      innerRadius={60}
      outerRadius={120}
      paddingAngle={2}
      dataKey="value"
    >
      {data.map((entry, index) => (
        <Cell
          key={`cell-${index}`}
          fill={`var(--chart-${(index % 5) + 1})`}
          className="transition-all duration-200 hover:opacity-80"
        />
      ))}
    </Pie>
    <Tooltip content={<CustomTooltip />} />
  </PieChart>
</ResponsiveContainer>
```

#### 图表色彩使用
- 优先使用 CSS 变量: `var(--chart-1)` 到 `var(--chart-5)`
- 支持暗色模式自动切换
- 悬停效果: `hover:opacity-80`

## 开发最佳实践

### 组件开发规范

#### 1. 使用 cn() 工具函数
```jsx
import { cn } from "@/lib/utils";

function MyComponent({ className, ...props }) {
  return (
    <div
      className={cn(
        "base-styles",
        "conditional-styles",
        className
      )}
      {...props}
    />
  );
}
```

#### 2. PropTypes 验证
```jsx
import PropTypes from 'prop-types';

MyComponent.propTypes = {
  isLoading: PropTypes.bool,
  data: PropTypes.array,
  onAction: PropTypes.func.isRequired
};
```

#### 3. 国际化支持
```jsx
import { useTranslation } from 'react-i18next';

function MyComponent() {
  const { t } = useTranslation();
  
  return (
    <h1>{t('dashboard_index.title')}</h1>
  );
}
```

#### 4. Loading 状态处理
```jsx
import { Skeleton } from '@/components/ui/skeleton';

function MyComponent({ isLoading, data }) {
  if (isLoading) {
    return (
      <Card>
        <CardHeader>
          <Skeleton className="h-6 w-48" />
        </CardHeader>
        <CardContent>
          <Skeleton className="h-80 w-full" />
        </CardContent>
      </Card>
    );
  }
  
  return (
    // 正常内容
  );
}
```

### 状态管理规范

#### Redux 使用
```jsx
import { useSelector, useDispatch } from 'react-redux';

function MyComponent() {
  const siteInfo = useSelector((state) => state.siteInfo);
  const dispatch = useDispatch();
  
  // 组件逻辑
}
```

#### 本地状态管理
```jsx
import { useState, useEffect } from 'react';

function MyComponent() {
  const [isLoading, setLoading] = useState(true);
  const [data, setData] = useState(null);
  
  useEffect(() => {
    // 数据获取逻辑
  }, []);
}
```

### API 调用规范

#### 使用统一的 API 工具
```jsx
import { API } from 'utils/api';
import { showError } from 'utils/common';

async function fetchData() {
  try {
    const response = await API.get('/api/endpoint');
    const { success, message, data } = response.data;
    
    if (success) {
      setData(data);
    } else {
      showError(message);
    }
  } catch (error) {
    console.error('API Error:', error);
    showError('请求失败');
  }
}
```

### 性能优化规范

#### 1. 组件懒加载
```jsx
import { lazy, Suspense } from 'react';

const LazyComponent = lazy(() => import('./LazyComponent'));

function App() {
  return (
    <Suspense fallback={<div>Loading...</div>}>
      <LazyComponent />
    </Suspense>
  );
}
```

#### 2. 图表性能优化
```jsx
// 使用 ResponsiveContainer 确保响应式
<ResponsiveContainer width="100%" height={300}>
  {/* 图表内容 */}
</ResponsiveContainer>

// 添加过渡动画
<Cell
  className="transition-all duration-200 hover:opacity-80"
/>
```

## 可访问性规范

### 语义化 HTML
```jsx
// 使用语义化标签
<main>
  <header>
    <h1>页面标题</h1>
  </header>
  <section>
    <h2>区块标题</h2>
  </section>
</main>
```

### ARIA 标签
```jsx
// 为交互元素添加 ARIA 标签
<button
  aria-label="关闭对话框"
  aria-expanded={isOpen}
  onClick={handleClose}
>
  <X className="h-4 w-4" />
</button>
```

### 键盘导航
```jsx
// 确保所有交互元素支持键盘导航
<button
  onKeyDown={(e) => {
    if (e.key === 'Enter' || e.key === ' ') {
      handleClick();
    }
  }}
>
  按钮
</button>
```

### 色彩对比度
- 确保文字与背景的对比度符合 WCAG 2.1 AA 标准
- 使用 `text-muted-foreground` 类提供足够的对比度
- 在暗色模式下验证所有色彩组合

## 代码质量要求

### 错误处理
```jsx
// 统一的错误处理
try {
  const result = await apiCall();
  setData(result);
} catch (error) {
  console.error('操作失败:', error);
  showError('操作失败，请重试');
}
```

### 类型安全
```jsx
// 使用 PropTypes 进行类型检查
Component.propTypes = {
  required: PropTypes.string.isRequired,
  optional: PropTypes.number,
  array: PropTypes.arrayOf(PropTypes.object)
};
```

### 代码注释
```jsx
/**
 * 模型使用饼图组件
 * @param {boolean} isLoading - 加载状态
 * @param {Array} data - 图表数据
 */
function ModelUsagePieChart({ isLoading, data }) {
  // 组件实现
}
```

## 部署和构建

### 环境配置
- 开发环境: `yarn dev` (端口 3010)
- 生产构建: `yarn build`
- 代码检查: `yarn lint`
- 代码格式化: `yarn prettier`

### 性能要求
- 首屏加载时间 < 3秒
- 图表渲染时间 < 1秒
- 支持暗色模式无缝切换
- 响应式设计支持移动端

---

**注意**: 本规范基于 CMSFullForm Dashboard 设计系统，确保所有新组件和页面都遵循这些标准。在开发过程中，优先使用 shadcn/ui 组件，逐步替换现有的 MUI 组件。
